<p style="color: green"><%= notice %></p>

<%= render @quiz %>

<div>
  <%= link_to "Iniciar quiz", quiz_quiz_realizations_path(@quiz), data: { "turbo-method": :post } %> |
  <%= link_to "Back to quizzes", quizzes_path %>
</div>

<h1>Questions</h1>

<div id="questions">
  <% @quiz.questions.each do |question| %>
    <%= render question %>
  <% end %>
</div>
